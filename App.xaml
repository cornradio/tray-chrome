<Application x:Class="TrayChrome.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar">
    <Application.Resources>
        <!-- 托盘图标定义 -->
        <tb:TaskbarIcon x:Key="TrayIcon"
                        IconSource="Resources/Ampeross-Ampola-Chrome.ico"
                        TrayLeftMouseUp="TrayIcon_TrayLeftMouseUp"
                        TrayMiddleMouseUp="TrayIcon_TrayMiddleMouseUp">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="极简模式" Name="SuperMinimalModeMenuItem" Click="SuperMinimalMode_Click" IsCheckable="True" />
                    <MenuItem Header="启用动画" Name="AnimationMenuItem" Click="Animation_Click" IsCheckable="True" IsChecked="True" />
                    <MenuItem Header="广告拦截" Name="AdBlockMenuItem" Click="AdBlock_Click" IsCheckable="True" />
                    <Separator />
                    <MenuItem Header="设置 (_S)" Click="OpenSettings_Click" />
                    <MenuItem Header="重置窗口位置 (_P)" Click="ResetWindowPosition_Click" />
                    <Separator />
                    <MenuItem Header="创建桌面快捷方式 (_C)" Click="CreateDesktopShortcut_Click" />
                    <Separator />
                    <MenuItem Header="增加实例 (_N)" Click="AddInstance_Click" />
                    <MenuItem Header="重启实例 (_R)" Click="RestartInstance_Click" />
                    <Separator />
                    <MenuItem Header="关闭实例 (_Q)" Click="CloseInstance_Click" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Application.Resources>
</Application>